@model IEnumerable<Salao.Web.Areas.Admin.Models.SalaoFormaPgtoModel>

@{
    ViewBag.Title = "Formas de pagamento";
}

<h2>Formas de pagamento</h2>

<div class="text-info">
    <h3>@ViewBag.Fantasia</h3>
</div>

@using (Html.BeginForm())
{
    @Html.AntiForgeryToken()
    @Html.Hidden("IdSalao", (int)@ViewBag.IdSalao)
    @Html.Hidden("IdEmpresa", (int)@ViewBag.IdEmpresa)
    <table class="table">
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.FomaPgto)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Selecionado)
            </th>
        </tr>

        @foreach (var item in Model)
	    {
		    <tr>
                <td>
                    @Html.DisplayFor(modelItem => item.FomaPgto)
                </td>
                <td>
                    <input type="checkbox" value="@item.IdFormaPgto" name="selecionado" id="selecionado" checked="@item.Selecionado" />
                </td>
            </tr>
	    }
    </table>

    <div class="form-group">
        <input type="submit" value="Gravar" class="btn btn-primary">
        @Html.ActionLink("Voltar", "Index", "Salao", new { idEmpresa = ViewBag.IdEmpresa }, null)
    </div>
}